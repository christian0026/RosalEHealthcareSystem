<UserControl x:Class="RosalEHealthcare.UI.WPF.Views.UserSettings.UserNotificationsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style x:Key="NotificationItem" TargetType="Border">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="50" Height="26">
                                <Border x:Name="track" Background="#E0E0E0" CornerRadius="13" Cursor="Hand"/>
                                <Border x:Name="thumb" Width="22" Height="22" CornerRadius="11" Background="White" HorizontalAlignment="Left" Margin="2,0,0,0" Cursor="Hand">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                            <ContentPresenter Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="#4CAF50"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="32,24">
        <StackPanel MaxWidth="800">

            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <iconPacks:PackIconMaterial Kind="BellOutline" Width="22" Height="22" Foreground="#2E7D32" VerticalAlignment="Center"/>
                        <TextBlock Text="General Notifications" Style="{StaticResource SectionTitle}" Margin="12,0,0,0"/>
                    </StackPanel>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#E8F5E9" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="Bell" Width="22" Height="22" Foreground="#2E7D32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Enable Notifications" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Master switch for all in-app notifications" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkEnableNotifications" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="True" Checked="ChkEnableNotifications_Changed" Unchecked="ChkEnableNotifications_Changed"/>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#E3F2FD" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="VolumeHigh" Width="22" Height="22" Foreground="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Sound Alerts" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Play sound when notifications arrive" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkSoundAlerts" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="True"/>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#FFF3E0" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="Monitor" Width="22" Height="22" Foreground="#F57C00" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Desktop Notifications" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Show notifications in system tray" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkDesktopNotifications" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="False"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <iconPacks:PackIconMaterial Kind="CalendarClock" Width="22" Height="22" Foreground="#1976D2" VerticalAlignment="Center"/>
                        <TextBlock Text="Appointment Notifications" Style="{StaticResource SectionTitle}" Margin="12,0,0,0"/>
                    </StackPanel>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#E8F5E9" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="AlarmCheck" Width="22" Height="22" Foreground="#2E7D32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Appointment Reminders" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Get notified about upcoming appointments" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkAppointmentReminders" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="True"/>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#E3F2FD" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="CalendarPlus" Width="22" Height="22" Foreground="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="New Appointment Alerts" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Get notified when new appointments are scheduled" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkNewAppointmentAlerts" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="True"/>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#FFEBEE" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="CalendarRemove" Width="22" Height="22" Foreground="#D32F2F" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Appointment Cancellations" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Get notified when appointments are cancelled" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkCancellationAlerts" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="True"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <iconPacks:PackIconMaterial Kind="Cog" Width="22" Height="22" Foreground="#9C27B0" VerticalAlignment="Center"/>
                        <TextBlock Text="System Notifications" Style="{StaticResource SectionTitle}" Margin="12,0,0,0"/>
                    </StackPanel>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#FFF3E0" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="PackageVariant" Width="22" Height="22" Foreground="#FF9800" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Low Stock Alerts" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Get notified when medicine stock is running low" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkLowStockAlerts" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="True"/>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#FFEBEE" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="TimerSand" Width="22" Height="22" Foreground="#D32F2F" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Expiring Medicine Alerts" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Get notified about medicines nearing expiration" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkExpiringAlerts" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="True"/>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource NotificationItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#F3E5F5" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="Update" Width="22" Height="22" Foreground="#7B1FA2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="System Updates" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Get notified about system updates and maintenance" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkSystemUpdates" Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="False"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnResetDefaults" Content="Reset to Defaults" Style="{StaticResource SecondaryButton}" Margin="0,0,12,0" Click="BtnResetDefaults_Click"/>
                <Button x:Name="BtnSaveNotifications" Style="{StaticResource PrimaryButton}" Click="BtnSaveNotifications_Click">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ContentSave" Width="18" Height="18"/>
                        <TextBlock Text="Save Settings" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
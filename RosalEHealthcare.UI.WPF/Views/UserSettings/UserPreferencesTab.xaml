<UserControl x:Class="RosalEHealthcare.UI.WPF.Views.UserSettings.UserPreferencesTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1000"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#388E3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="40">
        <StackPanel>
            <TextBlock Text="Preferences" FontSize="20" FontWeight="Bold" Foreground="#333" Margin="0,0,0,4"/>
            <TextBlock Text="Customize your experience" FontSize="13" Foreground="#888" Margin="0,0,0,24"/>

            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <iconPacks:PackIconMaterial Kind="MonitorEdit" Width="20" Height="20" Foreground="#2E7D32" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Display Preferences" Style="{StaticResource SectionTitle}" Margin="0"/>
                    </StackPanel>
                    <TextBlock Text="Customize visual settings" FontSize="12" Foreground="#888" Margin="0,0,0,24"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Column="0" Grid.Row="0">
                            <TextBlock Text="Items Per Page" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbItemsPerPage" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="10 items" Tag="10"/>
                                <ComboBoxItem Content="20 items" Tag="20" IsSelected="True"/>
                                <ComboBoxItem Content="30 items" Tag="30"/>
                                <ComboBoxItem Content="50 items" Tag="50"/>
                            </ComboBox>
                            <TextBlock Text="Number of records shown per page in lists" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Grid.Row="0">
                            <TextBlock Text="Date Format" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbDateFormat" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="MM/dd/yyyy (12/31/2024)" Tag="MM/dd/yyyy" IsSelected="True"/>
                                <ComboBoxItem Content="dd/MM/yyyy" Tag="dd/MM/yyyy"/>
                                <ComboBoxItem Content="yyyy-MM-dd" Tag="yyyy-MM-dd"/>
                            </ComboBox>
                            <TextBlock Text="Format used for displaying dates" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="2">
                            <TextBlock Text="Time Format" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbTimeFormat" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="12-hour (2:30 PM)" Tag="h:mm tt" IsSelected="True"/>
                                <ComboBoxItem Content="24-hour (14:30)" Tag="HH:mm"/>
                            </ComboBox>
                            <TextBlock Text="Format used for displaying times" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Grid.Row="2">
                            <TextBlock Text="Default Landing Page" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbDefaultPage" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Dashboard" Tag="Dashboard" IsSelected="True"/>
                                <ComboBoxItem Content="Patient Records" Tag="PatientRecords"/>
                                <ComboBoxItem Content="Appointments" Tag="Appointments"/>
                            </ComboBox>
                            <TextBlock Text="Page shown after login" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <iconPacks:PackIconMaterial Kind="TableLarge" Width="20" Height="20" Foreground="#FF9800" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Data Display" Style="{StaticResource SectionTitle}" Margin="0"/>
                    </StackPanel>
                    <TextBlock Text="Sorting and refreshing data" FontSize="12" Foreground="#888" Margin="0,0,0,24"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Default Sort Order" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbSortOrder" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Newest First" Tag="DESC" IsSelected="True"/>
                                <ComboBoxItem Content="Oldest First" Tag="ASC"/>
                                <ComboBoxItem Content="Alphabetical (A-Z)" Tag="ASC_ALPHA"/>
                            </ComboBox>
                            <TextBlock Text="Default order for lists and tables" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Auto Refresh Interval" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbAutoRefresh" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Disabled" Tag="0" IsSelected="True"/>
                                <ComboBoxItem Content="Every 1 minute" Tag="1"/>
                                <ComboBoxItem Content="Every 5 minutes" Tag="5"/>
                            </ComboBox>
                            <TextBlock Text="Automatically refresh dashboard data" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <iconPacks:PackIconMaterial Kind="AccessPoint" Width="20" Height="20" Foreground="#9C27B0" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Accessibility" Style="{StaticResource SectionTitle}" Margin="0"/>
                    </StackPanel>
                    <TextBlock Text="Appearance settings" FontSize="12" Foreground="#888" Margin="0,0,0,24"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Font Size" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbFontSize" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Medium (Default)" Tag="Medium" IsSelected="True"/>
                                <ComboBoxItem Content="Large" Tag="Large"/>
                            </ComboBox>
                            <TextBlock Text="Adjust text size throughout the application" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Animations" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbAnimations" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Enabled" Tag="True" IsSelected="True"/>
                                <ComboBoxItem Content="Disabled" Tag="False"/>
                            </ComboBox>
                            <TextBlock Text="Control UI animations and transitions" FontSize="11" Foreground="#999" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="#E3F2FD" BorderBrush="#90CAF9" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="InformationOutline" Width="18" Height="18" Foreground="#1976D2" VerticalAlignment="Top" Margin="0,2,8,0"/>
                    <TextBlock Foreground="#1565C0" FontSize="13" TextWrapping="Wrap">
                        <Run FontWeight="SemiBold">Note:</Run> 
                        <Run>Some preferences may require you to refresh the page or restart the application to take effect.</Run>
                    </TextBlock>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnResetDefaults" Style="{StaticResource SecondaryButton}" Content="Reset to Defaults" Margin="0,0,12,0" Click="BtnResetDefaults_Click"/>
                <Button x:Name="BtnSavePreferences" Style="{StaticResource PrimaryButton}" Click="BtnSavePreferences_Click">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ContentSave" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Save Preferences"/>
                    </StackPanel>
                </Button>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
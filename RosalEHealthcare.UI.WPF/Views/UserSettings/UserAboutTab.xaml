<UserControl x:Class="RosalEHealthcare.UI.WPF.Views.UserSettings.UserAboutTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1000"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
        </Style>

        <Style x:Key="InfoItem" TargetType="Border">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="HelpLinkButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FAFAFA"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="40">
        <StackPanel> 
            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <Grid Margin="0,0,0,32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="80" Height="80" CornerRadius="16" Background="#E8F5E9" Margin="0,0,24,0">
                            <Image Source="/RosalEHealthcare.UI.WPF;component/Resources/rosal_logo.png" Width="60" Height="60" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Rosal eHealthcare System" FontSize="22" FontWeight="Bold" Foreground="#2E7D32" Margin="0,0,0,4"/>
                            <TextBlock Text="Clinic Management Software" FontSize="14" Foreground="#666" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#E8F5E9" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                    <TextBlock x:Name="TxtVersion" Text="Version 1.0.0" FontSize="12" FontWeight="SemiBold" Foreground="#2E7D32"/>
                                </Border>
                                <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4">
                                    <TextBlock x:Name="TxtBuildDate" Text="Build: Dec 2025" FontSize="12" FontWeight="SemiBold" Foreground="#1976D2"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <Border Height="1" Background="#F0F0F0" Margin="0,0,0,24"/>

                    <TextBlock Text="System Information" FontSize="16" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource InfoItem}">
                            <StackPanel>
                                <TextBlock Text="Operating System" FontSize="11" Foreground="#888"/>
                                <TextBlock x:Name="TxtOS" Text="Microsoft Windows NT 10.0..." FontSize="13" FontWeight="SemiBold" Foreground="#333" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Grid.Row="0" Style="{StaticResource InfoItem}">
                            <StackPanel>
                                <TextBlock Text=".NET Framework" FontSize="11" Foreground="#888"/>
                                <TextBlock x:Name="TxtDotNet" Text="4.8.0" FontSize="13" FontWeight="SemiBold" Foreground="#333" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="0" Grid.Row="1" Style="{StaticResource InfoItem}">
                            <StackPanel>
                                <TextBlock Text="Machine Name" FontSize="11" Foreground="#888"/>
                                <TextBlock x:Name="TxtMachineName" Text="DESKTOP-PC" FontSize="13" FontWeight="SemiBold" Foreground="#333" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Grid.Row="1" Style="{StaticResource InfoItem}">
                            <StackPanel>
                                <TextBlock Text="Windows User" FontSize="11" Foreground="#888"/>
                                <TextBlock x:Name="TxtWindowsUser" Text="Admin" FontSize="13" FontWeight="SemiBold" Foreground="#333" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="0" Grid.Row="2" Style="{StaticResource InfoItem}">
                            <StackPanel>
                                <TextBlock Text="Memory Usage" FontSize="11" Foreground="#888"/>
                                <TextBlock x:Name="TxtMemoryUsage" Text="256 MB" FontSize="13" FontWeight="SemiBold" Foreground="#333" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Grid.Row="2" Style="{StaticResource InfoItem}">
                            <StackPanel>
                                <TextBlock Text="Session Duration" FontSize="11" Foreground="#888"/>
                                <TextBlock x:Name="TxtSessionDuration" Text="5m 30s" FontSize="13" FontWeight="SemiBold" Foreground="#333" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <TextBlock Text="Help &amp; Support" FontSize="16" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,16"/>

                    <Button x:Name="BtnUserManual" Style="{StaticResource HelpLinkButton}" Click="BtnUserManual_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#E3F2FD" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="BookOpenPageVariant" Width="20" Height="20" Foreground="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="User Manual" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="View complete documentation" FontSize="12" Foreground="#888" Margin="0,2,0,0"/>
                            </StackPanel>
                            <iconPacks:PackIconMaterial Grid.Column="2" Kind="ChevronRight" Width="20" Height="20" Foreground="#CCC" VerticalAlignment="Center"/>
                        </Grid>
                    </Button>

                    <Button x:Name="BtnContactSupport" Style="{StaticResource HelpLinkButton}" Click="BtnContactSupport_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#E8F5E9" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="Headset" Width="20" Height="20" Foreground="#4CAF50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Contact Support" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Get help from technical support" FontSize="12" Foreground="#888" Margin="0,2,0,0"/>
                            </StackPanel>
                            <iconPacks:PackIconMaterial Grid.Column="2" Kind="ChevronRight" Width="20" Height="20" Foreground="#CCC" VerticalAlignment="Center"/>
                        </Grid>
                    </Button>

                    <Button x:Name="BtnReportBug" Style="{StaticResource HelpLinkButton}" Click="BtnReportBug_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#FFEBEE" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="Bug" Width="20" Height="20" Foreground="#F44336" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Report a Bug" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Help us improve by reporting issues" FontSize="12" Foreground="#888" Margin="0,2,0,0"/>
                            </StackPanel>
                            <iconPacks:PackIconMaterial Grid.Column="2" Kind="ChevronRight" Width="20" Height="20" Foreground="#CCC" VerticalAlignment="Center"/>
                        </Grid>
                    </Button>

                    <Button x:Name="BtnFeatureRequest" Style="{StaticResource HelpLinkButton}" Click="BtnFeatureRequest_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#F3E5F5" Margin="0,0,16,0">
                                <iconPacks:PackIconMaterial Kind="Lightbulb" Width="20" Height="20" Foreground="#9C27B0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Feature Request" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Suggest new features or improvements" FontSize="12" Foreground="#888" Margin="0,2,0,0"/>
                            </StackPanel>
                            <iconPacks:PackIconMaterial Grid.Column="2" Kind="ChevronRight" Width="20" Height="20" Foreground="#CCC" VerticalAlignment="Center"/>
                        </Grid>
                    </Button>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
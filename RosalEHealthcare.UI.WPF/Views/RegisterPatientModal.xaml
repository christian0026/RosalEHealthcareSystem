<Window x:Class="RosalEHealthcare.UI.WPF.Views.RegisterPatientModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Quick Patient Registration"
        Width="900"
        Height="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="20,0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="#80000000" CornerRadius="0">
        <Border Background="White" 
                Width="850"
                CornerRadius="12"
                Margin="25">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="0" Opacity="0.3"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" 
                        Background="#2E7D32" 
                        CornerRadius="12,12,0,0"
                        Padding="30,20">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="📝 Quick Patient Registration" 
                                       FontSize="22" 
                                       FontWeight="Bold" 
                                       Foreground="White"/>
                            <TextBlock Text="Register a new patient and automatically schedule today's appointment" 
                                       FontSize="13" 
                                       Foreground="#E0E0E0"
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                        <Button Content="✕" 
                                Width="35" 
                                Height="35" 
                                FontSize="18"
                                Background="Transparent" 
                                Foreground="White"
                                BorderThickness="0" 
                                Cursor="Hand"
                                HorizontalAlignment="Right"
                                Click="CloseButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="17.5">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#388E3C"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>

                <!-- Form Content -->
                <ScrollViewer Grid.Row="1" 
                              VerticalScrollBarVisibility="Auto"
                              Padding="30,20">
                    <StackPanel>
                        <!-- Personal Information Section -->
                        <TextBlock Text="Personal Information" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="#2E7D32"
                                   Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Full Name *" 
                                           FontSize="13" 
                                           FontWeight="SemiBold"
                                           Foreground="#666"
                                           Margin="0,0,0,6"/>
                                <TextBox x:Name="txtFullName" 
                                         Style="{StaticResource ModernTextBox}"
                                         materialDesign:HintAssist.Hint="Enter full name"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Contact Number *" 
                                           FontSize="13" 
                                           FontWeight="SemiBold"
                                           Foreground="#666"
                                           Margin="0,0,0,6"/>
                                <TextBox x:Name="txtContact" 
                                         Style="{StaticResource ModernTextBox}"
                                         materialDesign:HintAssist.Hint="09XXXXXXXXX"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Birth Date *" 
                                           FontSize="13" 
                                           FontWeight="SemiBold"
                                           Foreground="#666"
                                           Margin="0,0,0,6"/>
                                <DatePicker x:Name="dpBirthDate" 
                                            Style="{StaticResource ModernDatePicker}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Gender *" 
                                           FontSize="13" 
                                           FontWeight="SemiBold"
                                           Foreground="#666"
                                           Margin="0,0,0,6"/>
                                <ComboBox x:Name="cmbGender" 
                                          Style="{StaticResource ModernComboBox}">
                                    <ComboBoxItem Content="Male"/>
                                    <ComboBoxItem Content="Female"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>

                        <!-- Contact Information Section -->
                        <TextBlock Text="Contact Information" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="#2E7D32"
                                   Margin="0,20,0,15"/>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Email Address" 
                                       FontSize="13" 
                                       FontWeight="SemiBold"
                                       Foreground="#666"
                                       Margin="0,0,0,6"/>
                            <TextBox x:Name="txtEmail" 
                                     Style="{StaticResource ModernTextBox}"
                                     materialDesign:HintAssist.Hint="patient@example.com"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Complete Address *" 
                                       FontSize="13" 
                                       FontWeight="SemiBold"
                                       Foreground="#666"
                                       Margin="0,0,0,6"/>
                            <TextBox x:Name="txtAddress" 
                                     Style="{StaticResource ModernTextBox}"
                                     Height="65"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     materialDesign:HintAssist.Hint="Street, Barangay, City, Province"/>
                        </StackPanel>

                        <!-- Medical Information Section -->
                        <TextBlock Text="Medical Information" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="#2E7D32"
                                   Margin="0,20,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Blood Type" 
                                           FontSize="13" 
                                           FontWeight="SemiBold"
                                           Foreground="#666"
                                           Margin="0,0,0,6"/>
                                <ComboBox x:Name="cmbBloodType" 
                                          Style="{StaticResource ModernComboBox}">
                                    <ComboBoxItem Content="A+"/>
                                    <ComboBoxItem Content="A-"/>
                                    <ComboBoxItem Content="B+"/>
                                    <ComboBoxItem Content="B-"/>
                                    <ComboBoxItem Content="AB+"/>
                                    <ComboBoxItem Content="AB-"/>
                                    <ComboBoxItem Content="O+"/>
                                    <ComboBoxItem Content="O-"/>
                                    <ComboBoxItem Content="Unknown"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Primary Diagnosis" 
                                           FontSize="13" 
                                           FontWeight="SemiBold"
                                           Foreground="#666"
                                           Margin="0,0,0,6"/>
                                <TextBox x:Name="txtPrimaryDiagnosis" 
                                         Style="{StaticResource ModernTextBox}"
                                         materialDesign:HintAssist.Hint="e.g., Hypertension"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Known Allergies" 
                                       FontSize="13" 
                                       FontWeight="SemiBold"
                                       Foreground="#666"
                                       Margin="0,0,0,6"/>
                            <TextBox x:Name="txtAllergies" 
                                     Style="{StaticResource ModernTextBox}"
                                     Height="65"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     materialDesign:HintAssist.Hint="List any known allergies or write 'None'"/>
                        </StackPanel>

                        <!-- Error Message -->
                        <Border x:Name="errorBorder" 
                                Background="#FFEBEE" 
                                BorderBrush="#F44336"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12"
                                Margin="0,10,0,0"
                                Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚠" 
                                           FontSize="16" 
                                           Foreground="#F44336"
                                           Margin="0,0,8,0"
                                           VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtError" 
                                           Text="Please fill in all required fields"
                                           FontSize="13"
                                           Foreground="#F44336"
                                           TextWrapping="Wrap"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- Footer Actions -->
                <Border Grid.Row="2" 
                        BorderBrush="#E0E0E0"
                        BorderThickness="0,1,0,0"
                        Padding="30,20"
                        Background="#FAFAFA"
                        CornerRadius="0,0,12,12">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="* Required fields" 
                                       FontSize="12" 
                                       Foreground="#999"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Right">
                            <Button Content="Cancel" 
                                    Width="120"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="0,0,10,0"
                                    Click="CancelButton_Click"/>
                            <Button x:Name="btnRegister"
                                    Content="✓ Register Patient" 
                                    Width="180"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="RegisterButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>
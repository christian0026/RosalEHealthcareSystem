<UserControl x:Class="RosalEHealthcare.UI.WPF.Views.Settings.GeneralSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="White">

    <UserControl.Resources>
        <!-- Section Card Style -->
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Section Title Style -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Section Subtitle Style -->
        <Style x:Key="SectionSubtitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Field Label Style -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- Text Input Style -->
        <Style x:Key="TextInput" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Padding="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2E7D32"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ComboInput" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#388E3C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1B5E20"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#BDBDBD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle Switch Style -->
        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="50" Height="26">
                                <Border x:Name="track"
                                        Background="#E0E0E0"
                                        CornerRadius="13"
                                        Cursor="Hand"/>
                                <Border x:Name="thumb"
                                        Width="22" Height="22"
                                        CornerRadius="11"
                                        Background="White"
                                        HorizontalAlignment="Left"
                                        Margin="2,0,0,0"
                                        Cursor="Hand">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="#4CAF50"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="track" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="24">
        <StackPanel>

            <!-- CLINIC INFORMATION Section -->
            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <iconPacks:PackIconMaterial Kind="HospitalBuilding" 
                                                    Width="20" Height="20" 
                                                    Foreground="#2E7D32"
                                                    VerticalAlignment="Center"/>
                        <TextBlock Text="Clinic Information" 
                                   Style="{StaticResource SectionTitle}"
                                   Margin="10,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="Basic information about your clinic" 
                               Style="{StaticResource SectionSubtitle}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Clinic Name -->
                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,16">
                            <TextBlock Text="Clinic Name" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="TxtClinicName" 
                                     Style="{StaticResource TextInput}"
                                     MaxLength="200"/>
                        </StackPanel>

                        <!-- Contact Number -->
                        <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,16">
                            <TextBlock Text="Contact Number" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="TxtContactNumber" 
                                     Style="{StaticResource TextInput}"
                                     MaxLength="50"/>
                        </StackPanel>

                        <!-- Address (Full Width) -->
                        <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3">
                            <TextBlock Text="Address" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="TxtAddress" 
                                     Style="{StaticResource TextInput}"
                                     Height="60"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalContentAlignment="Top"
                                     MaxLength="500"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- REGIONAL SETTINGS Section -->
            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <iconPacks:PackIconMaterial Kind="Earth" 
                                                    Width="20" Height="20" 
                                                    Foreground="#1976D2"
                                                    VerticalAlignment="Center"/>
                        <TextBlock Text="Regional Settings" 
                                   Style="{StaticResource SectionTitle}"
                                   Margin="10,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="Date, time, and timezone preferences" 
                               Style="{StaticResource SectionSubtitle}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Date Format -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Date Format" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbDateFormat" Style="{StaticResource ComboInput}">
                                <ComboBoxItem Content="MM/dd/yyyy" Tag="MM/dd/yyyy"/>
                                <ComboBoxItem Content="dd/MM/yyyy" Tag="dd/MM/yyyy"/>
                                <ComboBoxItem Content="yyyy-MM-dd" Tag="yyyy-MM-dd"/>
                                <ComboBoxItem Content="MMMM dd, yyyy" Tag="MMMM dd, yyyy"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Time Format -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Time Format" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbTimeFormat" Style="{StaticResource ComboInput}">
                                <ComboBoxItem Content="12-hour (AM/PM)" Tag="12"/>
                                <ComboBoxItem Content="24-hour" Tag="24"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Timezone -->
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Timezone" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbTimezone" Style="{StaticResource ComboInput}">
                                <ComboBoxItem Content="Asia/Manila (GMT+8)" Tag="Asia/Manila"/>
                                <ComboBoxItem Content="Asia/Singapore (GMT+8)" Tag="Asia/Singapore"/>
                                <ComboBoxItem Content="Asia/Hong_Kong (GMT+8)" Tag="Asia/Hong_Kong"/>
                                <ComboBoxItem Content="Asia/Tokyo (GMT+9)" Tag="Asia/Tokyo"/>
                                <ComboBoxItem Content="UTC (GMT+0)" Tag="UTC"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- SYSTEM PREFERENCES Section -->
            <Border Style="{StaticResource SectionCard}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <iconPacks:PackIconMaterial Kind="Cog" 
                                                    Width="20" Height="20" 
                                                    Foreground="#FF9800"
                                                    VerticalAlignment="Center"/>
                        <TextBlock Text="System Preferences" 
                                   Style="{StaticResource SectionTitle}"
                                   Margin="10,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="Customize system behavior and display options" 
                               Style="{StaticResource SectionSubtitle}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Default Landing Page -->
                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Default Landing Page" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbDefaultPage" Style="{StaticResource ComboInput}">
                                <ComboBoxItem Content="Dashboard" Tag="Dashboard"/>
                                <ComboBoxItem Content="Patient Management" Tag="PatientManagement"/>
                                <ComboBoxItem Content="Appointments" Tag="Appointments"/>
                                <ComboBoxItem Content="Medicine Inventory" Tag="MedicineInventory"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Items Per Page -->
                        <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Items Per Page (Pagination)" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbItemsPerPage" Style="{StaticResource ComboInput}">
                                <ComboBoxItem Content="10 items" Tag="10"/>
                                <ComboBoxItem Content="25 items" Tag="25"/>
                                <ComboBoxItem Content="50 items" Tag="50"/>
                                <ComboBoxItem Content="100 items" Tag="100"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Auto-Refresh Interval -->
                        <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,20">
                            <TextBlock Text="Dashboard Auto-Refresh" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="CmbAutoRefresh" Style="{StaticResource ComboInput}">
                                <ComboBoxItem Content="Off" Tag="0"/>
                                <ComboBoxItem Content="Every 30 seconds" Tag="30"/>
                                <ComboBoxItem Content="Every 1 minute" Tag="60"/>
                                <ComboBoxItem Content="Every 5 minutes" Tag="300"/>
                                <ComboBoxItem Content="Every 10 minutes" Tag="600"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Sound Notifications -->
                        <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,20">
                            <TextBlock Text="Sound Notifications" Style="{StaticResource FieldLabel}"/>
                            <CheckBox x:Name="ChkSoundNotifications" 
                                      Style="{StaticResource ToggleSwitch}"
                                      Margin="0,8,0,0">
                                <TextBlock Text="Play sounds for alerts and notifications" 
                                           Foreground="#666666" 
                                           FontSize="13"/>
                            </CheckBox>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Action Buttons -->
            <Border Background="#F5F5F5" 
                    CornerRadius="8" 
                    Padding="20" 
                    Margin="0,8,0,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button x:Name="BtnResetDefaults" 
                                Style="{StaticResource SecondaryButton}"
                                Click="BtnResetDefaults_Click">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Restore" 
                                                            Width="16" Height="16" 
                                                            VerticalAlignment="Center"/>
                                <TextBlock Text="Reset to Defaults" Margin="8,0,0,0"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="BtnCancel" 
                                Style="{StaticResource SecondaryButton}"
                                Click="BtnCancel_Click"
                                Margin="0,0,12,0">
                            <TextBlock Text="Cancel"/>
                        </Button>
                        <Button x:Name="BtnSave" 
                                Style="{StaticResource PrimaryButton}"
                                Click="BtnSave_Click">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="ContentSave" 
                                                            Width="16" Height="16" 
                                                            VerticalAlignment="Center"/>
                                <TextBlock Text="Save Changes" Margin="8,0,0,0"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
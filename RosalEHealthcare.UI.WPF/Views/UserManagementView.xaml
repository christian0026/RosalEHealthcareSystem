<UserControl x:Class="RosalEHealthcare.UI.WPF.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:RosalEHealthcare.UI.WPF.Converters"
             xmlns:local="clr-namespace:RosalEHealthcare.UI.WPF.Controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F5F5F5"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="StrNullOrEmptyToVis"/>
        <!-- Search TextBox Style -->
        <Style x:Key="SearchTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          VerticalAlignment="Center"
                                          Margin="12,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Filter ComboBox Style -->
        <Style x:Key="FilterComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#388E3C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1B5E20"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Styles -->
        <Style x:Key="ViewButton" TargetType="Button">
            <Setter Property="Background" Value="#0288D1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0277BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditButton" TargetType="Button">
            <Setter Property="Background" Value="#FFA000"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF8F00"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button">
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C62828"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pagination Button Style -->
        <Style x:Key="PaginationButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PaginationButtonActive" TargetType="Button" BasedOn="{StaticResource PaginationButton}">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2E7D32"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Summary Cards -->
        <Grid Grid.Row="0" Margin="24,24,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:SummaryCard x:Name="CardTotalUsers"
                               Grid.Column="0"
                               Margin="0,0,12,0"
                               Title="Total Users"
                               Value="0"
                               Icon="👥"
                               TopColor="#2E7D32"
                               IconBackground="#E8F5E9"
                               TrendText="System users"
                               TrendIcon="📊"
                               TrendColor="#4CAF50"/>

            <local:SummaryCard x:Name="CardDoctors"
                               Grid.Column="1"
                               Margin="6,0"
                               Title="Doctors"
                               Value="0"
                               Icon="👨‍⚕️"
                               TopColor="#1976D2"
                               IconBackground="#E3F2FD"
                               TrendText="Active practitioners"
                               TrendIcon="🏥"
                               TrendColor="#2196F3"/>

            <local:SummaryCard x:Name="CardReceptionists"
                               Grid.Column="2"
                               Margin="6,0"
                               Title="Receptionists"
                               Value="0"
                               Icon="💼"
                               TopColor="#F57C00"
                               IconBackground="#FFF3E0"
                               TrendText="Front desk staff"
                               TrendIcon="📋"
                               TrendColor="#FF9800"/>

            <local:SummaryCard x:Name="CardAdmins"
                               Grid.Column="3"
                               Margin="12,0,0,0"
                               Title="Administrators"
                               Value="0"
                               Icon="🔐"
                               TopColor="#C62828"
                               IconBackground="#FFEBEE"
                               TrendText="System management"
                               TrendIcon="⚙️"
                               TrendColor="#F44336"/>
        </Grid>

        <!-- Header with Add Button -->
        <Grid Grid.Row="1" Margin="24,24,24,0">
            <TextBlock Text="User Records" 
                       FontSize="20" 
                       FontWeight="Bold"
                       Foreground="#333"
                       VerticalAlignment="Center"/>
            <Button x:Name="BtnAddUser"
                    Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Right"
                    Click="AddUser_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Plus" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="ADD NEW USER" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Search & Filters -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="12" 
                Padding="20"
                Margin="24,16,24,0">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MaxWidth="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Grid Grid.Column="0">
                    <TextBox x:Name="SearchBox" 
                             Style="{StaticResource SearchTextBox}"
                             TextChanged="SearchBox_TextChanged"/>
                    <TextBlock Text="🔍 Search by name, email, or user ID..."
                               IsHitTestVisible="False"
                               Foreground="#9E9E9E"
                               FontSize="13"
                               Margin="40,0,0,0"
                               VerticalAlignment="Center"
                               Visibility="{Binding Text, ElementName=SearchBox, Converter={StaticResource StrNullOrEmptyToVis}}"/>
                </Grid>

                <!-- Role Filter -->
                <ComboBox x:Name="RoleFilter" 
                          Grid.Column="1"
                          Style="{StaticResource FilterComboBox}"
                          Width="160"
                          Margin="16,0,0,0"
                          SelectionChanged="Filter_SelectionChanged">
                    <ComboBoxItem Content="All Roles" IsSelected="True"/>
                    <ComboBoxItem Content="Administrator"/>
                    <ComboBoxItem Content="Doctor"/>
                    <ComboBoxItem Content="Receptionist"/>
                </ComboBox>

                <!-- Status Filter -->
                <ComboBox x:Name="StatusFilter" 
                          Grid.Column="2"
                          Style="{StaticResource FilterComboBox}"
                          Width="160"
                          Margin="12,0,0,0"
                          SelectionChanged="Filter_SelectionChanged">
                    <ComboBoxItem Content="All Status" IsSelected="True"/>
                    <ComboBoxItem Content="Active"/>
                    <ComboBoxItem Content="Inactive"/>
                    <ComboBoxItem Content="Locked"/>
                    <ComboBoxItem Content="Pending"/>
                </ComboBox>

                <!-- Clear Filters -->
                <Button x:Name="BtnClearFilters"
                        Grid.Column="3"
                        Content="Clear"
                        Background="#F5F5F5"
                        Foreground="#666"
                        FontSize="13"
                        Height="40"
                        Padding="16,8"
                        Margin="12,0,0,0"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="ClearFilters_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Users List -->
        <Border Grid.Row="3" 
                Background="White" 
                CornerRadius="12" 
                Margin="24,16,24,24">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- List Header -->
                <Border Grid.Row="0" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="20,16">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Users List" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="#333"/>
                            <TextBlock x:Name="TxtResultCount"
                                       Text=" (0 users)"
                                       FontSize="14"
                                       Foreground="#757575"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- DataGrid with ScrollViewer -->
                <DataGrid x:Name="UsersDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="None"
                  BorderThickness="0"
                  Background="White"
                  RowBackground="White"
                  AlternatingRowBackground="#FAFAFA"
                  RowHeight="56"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#333"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" 
                            Background="{TemplateBinding Background}"
                            BorderThickness="0">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E8F5E9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <!-- User Info with Avatar -->
                        <DataGridTemplateColumn Header="User Info" Width="200" MinWidth="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Grid Width="36" Height="36" Margin="0,0,12,0">
                                            <Border CornerRadius="18" Background="#E8F5E9">
                                                <TextBlock Text="{Binding Initials}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#2E7D32"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FullName}" 
                                   FontWeight="SemiBold" 
                                   FontSize="13"
                                   Foreground="#333"/>
                                            <TextBlock Text="{Binding Email}" 
                                   FontSize="11"
                                   Foreground="#757575"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Username -->
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#555"/>
                                    <Setter Property="FontSize" Value="13"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- User Code -->
                        <DataGridTextColumn Header="User ID" Binding="{Binding UserCode}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#555"/>
                                    <Setter Property="FontSize" Value="13"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Role with Badge -->
                        <DataGridTemplateColumn Header="Role" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding RoleBadgeBackground}"
                        CornerRadius="4"
                        Padding="8,4"
                        HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Role}"
                               Foreground="{Binding RoleBadgeColor}"
                               FontSize="11"
                               FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Last Login -->
                        <DataGridTextColumn Header="Last Login" Binding="{Binding LastLoginFormatted}" Width="130">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#555"/>
                                    <Setter Property="FontSize" Value="12"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Status with Badge -->
                        <DataGridTemplateColumn Header="Status" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding StatusBadgeBackground}"
                        CornerRadius="4"
                        Padding="8,4"
                        HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Status}"
                               Foreground="{Binding StatusBadgeColor}"
                               FontSize="11"
                               FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Actions -->
                        <DataGridTemplateColumn Header="Actions" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <Button Content="VIEW" Style="{StaticResource ViewButton}" Click="ViewUser_Click" Tag="{Binding Id}"/>
                                        <Button Content="EDIT" Style="{StaticResource EditButton}" Click="EditUser_Click" Tag="{Binding Id}"/>
                                        <Button Content="DELETE" Style="{StaticResource DeleteButton}" Click="DeleteUser_Click" Tag="{Binding Id}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Pagination -->
                <Border Grid.Row="2" BorderBrush="#F0F0F0" BorderThickness="0,1,0,0" Padding="20,16">
                    <Grid>
                        <TextBlock x:Name="TxtPageInfo"
                                   Text="Showing 1-10 of 100 users"
                                   FontSize="13"
                                   Foreground="#757575"
                                   VerticalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="BtnPrevPage" 
                                    Style="{StaticResource PaginationButton}"
                                    Click="PrevPage_Click"
                                    Margin="0,0,8,0"
                                    ToolTip="Previous Page">
                                <iconPacks:PackIconMaterial Kind="ChevronLeft" Width="14" Height="14"/>
                            </Button>

                            <Button x:Name="BtnPage1" Content="1" Style="{StaticResource PaginationButtonActive}" Margin="2,0" Click="PageNumber_Click"/>
                            <Button x:Name="BtnPage2" Content="2" Style="{StaticResource PaginationButton}" Margin="2,0" Click="PageNumber_Click"/>
                            <Button x:Name="BtnPage3" Content="3" Style="{StaticResource PaginationButton}" Margin="2,0" Click="PageNumber_Click"/>
                            <TextBlock x:Name="TxtEllipsis" Text="..." VerticalAlignment="Center" Margin="6,0" Foreground="#757575"/>
                            <Button x:Name="BtnPageLast" Content="10" Style="{StaticResource PaginationButton}" Margin="2,0" Click="PageNumber_Click"/>

                            <Button x:Name="BtnNextPage" 
                                    Style="{StaticResource PaginationButton}"
                                    Click="NextPage_Click"
                                    Margin="8,0,0,0"
                                    ToolTip="Next Page">
                                <iconPacks:PackIconMaterial Kind="ChevronRight" Width="14" Height="14"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" 
                Grid.RowSpan="4"
                Background="#80FFFFFF" 
                Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading..." FontSize="16" Foreground="#333"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
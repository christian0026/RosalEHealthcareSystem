<UserControl x:Class="RosalEHealthcare.UI.WPF.Views.PatientManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:RosalEHealthcare.UI.WPF.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:RosalEHealthcare.UI.WPF.Views"
             Background="#F0F2F5"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:BoolToArchiveStatusConverter x:Key="BoolToArchiveText"/>
        <Style TargetType="ScrollBar" BasedOn="{StaticResource MaterialDesignScrollBarMinimal}"/>
    </UserControl.Resources>

    <Grid>
        <Border x:Name="LoadingOverlay" Background="#99FFFFFF" Visibility="Collapsed" Panel.ZIndex="999">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True" Value="0" Width="50" Height="50" Margin="0,0,0,16"
                             Foreground="{StaticResource PrimaryHueMidBrush}"/>
                <TextBlock Text="Loading Patients..." Foreground="{StaticResource PrimaryHueMidBrush}" FontSize="16" FontWeight="Medium"/>
            </StackPanel>
        </Border>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,24">
                <controls:SummaryCard x:Name="CardTotalPatients" Title="Total Patients" Value="0" Icon="👥" TopColor="#4CAF50" IconBackground="#E8F5E9" Margin="0,0,16,0"/>
                <controls:SummaryCard x:Name="CardTodayAppointments" Title="Today's Visits" Value="0" Icon="📅" TopColor="#2196F3" IconBackground="#E3F2FD" Margin="0,0,16,0"/>
                <controls:SummaryCard x:Name="CardPendingAppointments" Title="Pending Checkups" Value="0" Icon="⏳" TopColor="#FF9800" IconBackground="#FFF3E0" Margin="0,0,16,0"/>
                <controls:SummaryCard x:Name="CardNewPatients" Title="New (This Month)" Value="0" Icon="➕" TopColor="#9C27B0" IconBackground="#F3E5F5" TrendText="Growth" TrendColor="#9C27B0" TrendIcon="📈" Margin="0"/>
            </UniformGrid>

            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:ColorZone Mode="Standard" Padding="8,4" CornerRadius="8" Panel.ZIndex="1" Margin="0,0,16,0" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="Magnify" Opacity=".56" VerticalAlignment="Center" Margin="8,0"/>
                            <TextBox x:Name="txtSearch" Grid.Column="1" Margin="8,0,0,0" materialDesign:HintAssist.Hint="Search patients..." materialDesign:TextFieldAssist.DecorationVisibility="Hidden" BorderThickness="0" MinWidth="250" VerticalAlignment="Center" TextChanged="txtSearch_TextChanged"/>
                        </Grid>
                    </materialDesign:ColorZone>

                    <ComboBox x:Name="cmbStatus" materialDesign:HintAssist.Hint="Status" Style="{StaticResource MaterialDesignOutlinedComboBox}" Padding="12,10" MinWidth="150" Margin="0,0,12,0" SelectionChanged="Filter_Changed">
                        <ComboBoxItem Content="All Status" IsSelected="True"/>
                        <ComboBoxItem Content="Active"/>
                        <ComboBoxItem Content="Pending"/>
                        <ComboBoxItem Content="Archived"/>
                    </ComboBox>

                    <ComboBox x:Name="cmbGender" materialDesign:HintAssist.Hint="Gender" Style="{StaticResource MaterialDesignOutlinedComboBox}" Padding="12,10" MinWidth="130" Margin="0,0,12,0" SelectionChanged="Filter_Changed">
                        <ComboBoxItem Content="All Gender" IsSelected="True"/>
                        <ComboBoxItem Content="Male"/>
                        <ComboBoxItem Content="Female"/>
                    </ComboBox>

                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Clear Filters" Click="BtnClearFilters_Click">
                        <materialDesign:PackIcon Kind="FilterOff" Opacity="0.6"/>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <materialDesign:PopupBox x:Name="btnExport" ToolTip="Tools &amp; Exports" Margin="0,0,12,0" PlacementMode="BottomAndAlignRightEdges">
                        <materialDesign:PopupBox.ToggleContent>
                            <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" CornerRadius="4" Padding="12,8" Background="Transparent">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Tools" Margin="0,0,8,0" VerticalAlignment="Center" Opacity="0.7"/>
                                    <TextBlock Text="Tools" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </Border>
                        </materialDesign:PopupBox.ToggleContent>
                        <StackPanel>
                            <Button Click="BtnExportExcel_Click">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileExcel" Margin="0,0,8,0" />
                                    <TextBlock Text="Export to Excel"/>
                                </StackPanel>
                            </Button>
                            <Button Click="BtnExportPdf_Click">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FilePdfBox" Margin="0,0,8,0"/>
                                    <TextBlock Text="Export to PDF"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </materialDesign:PopupBox>

                    <Button x:Name="btnAddPatient" Click="BtnAddPatient_Click" Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountPlus" Margin="0,0,8,0"/>
                            <TextBlock Text="Add Patient" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <materialDesign:Card Grid.Row="2" Background="White" UniformCornerRadius="8" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="16">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountDetails" VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.7"/>
                                <TextBlock Text="Patient Records" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock x:Name="txtResultCount" Text="Showing 0 patients" HorizontalAlignment="Right" FontSize="12" Opacity="0.6" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <DataGrid x:Name="dgPatients" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                              HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="FullRow" GridLinesVisibility="Horizontal" 
                              HorizontalGridLinesBrush="#F0F0F0" RowHeight="60" Background="Transparent" BorderThickness="0">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="16,12"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="16,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Patient Name" Width="2.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:ColorZone Mode="PrimaryLight" Width="40" Height="40" CornerRadius="20" Margin="0,0,12,0">
                                                <TextBlock Text="{Binding Initials}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                            </materialDesign:ColorZone>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" FontSize="14"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding PatientId}" FontSize="11" Opacity="0.6" Margin="0,0,8,0"/>
                                                    <TextBlock Text="{Binding Gender}" FontSize="11" Opacity="0.6"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Contact" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Contact}" FontSize="13"/>
                                            <TextBlock Text="{Binding Email}" FontSize="11" Opacity="0.6"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Last Visit" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding LastVisitFormatted}" FontSize="13"/>
                                            <TextBlock Text="{Binding PrimaryDiagnosis}" FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Status" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Center" Background="{Binding StatusBackground}">
                                            <TextBlock Text="{Binding Status}" Foreground="{Binding StatusForeground}" FontSize="11" FontWeight="SemiBold"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Actions" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="View Details" Width="30" Height="30" Click="BtnView_Click" Tag="{Binding Id}">
                                                <materialDesign:PackIcon Kind="Eye" Width="16" Height="16" Foreground="#0288D1"/>
                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Edit" Width="30" Height="30" Click="BtnEdit_Click" Tag="{Binding Id}">
                                                <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16" Foreground="#FFA000"/>
                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Archive" Width="30" Height="30" Click="BtnArchive_Click" Tag="{Binding Id}">
                                                <materialDesign:PackIcon Kind="Archive" Width="16" Height="16" Foreground="#D32F2F"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </materialDesign:Card>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <TextBlock x:Name="txtPageInfo" Text="Page 1 of 1" VerticalAlignment="Center" Margin="0,0,16,0" Opacity="0.6"/>
                <Button x:Name="btnFirst" Click="BtnFirst_Click" Style="{StaticResource MaterialDesignIconButton}" ToolTip="First Page">
                    <materialDesign:PackIcon Kind="PageFirst"/>
                </Button>
                <Button x:Name="btnPrevious" Click="BtnPrevious_Click" Style="{StaticResource MaterialDesignIconButton}" ToolTip="Previous">
                    <materialDesign:PackIcon Kind="ChevronLeft"/>
                </Button>
                <Button x:Name="btnNext" Click="BtnNext_Click" Style="{StaticResource MaterialDesignIconButton}" ToolTip="Next">
                    <materialDesign:PackIcon Kind="ChevronRight"/>
                </Button>
                <Button x:Name="btnLast" Click="BtnLast_Click" Style="{StaticResource MaterialDesignIconButton}" ToolTip="Last Page">
                    <materialDesign:PackIcon Kind="PageLast"/>
                </Button>
            </StackPanel>
        </Grid>

        <Grid x:Name="viewDialog" Background="#99000000" Visibility="Collapsed" Panel.ZIndex="1000">
            <materialDesign:Card Width="700" Height="500" UniformCornerRadius="12" VerticalAlignment="Center" HorizontalAlignment="Center" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="20" BorderBrush="#EEE" BorderThickness="0,0,0,1">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Patient Details" FontSize="20" FontWeight="Bold"/>
                                <TextBlock x:Name="lblViewPatientId" Text="ID: PT-2024-001" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignIconForegroundButton}" Click="CloseViewDialog_Click">
                                <materialDesign:PackIcon Kind="Close"/>
                            </Button>
                        </Grid>
                    </Border>

                    <TabControl Grid.Row="1" Style="{StaticResource MaterialDesignTabControl}" materialDesign:ColorZoneAssist.Mode="Standard" Padding="0">
                        <TabItem Header="Profile">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                                <StackPanel x:Name="patientDetailsPanel"/>
                            </ScrollViewer>
                        </TabItem>
                        <TabItem Header="Medical History">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="MedicalBag" Width="40" Height="40" Foreground="#DDD" HorizontalAlignment="Center"/>
                                <TextBlock Text="History Feature Coming Soon" Foreground="Gray" Margin="0,10,0,0"/>
                            </StackPanel>
                        </TabItem>
                    </TabControl>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>